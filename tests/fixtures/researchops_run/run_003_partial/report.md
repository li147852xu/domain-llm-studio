# Partial Run

This run only has a report. plan.json / sources.jsonl / evidence_map.json are
intentionally missing — importer should still produce ``fin_summary`` samples
where possible and skip the other 3 task types without raising.

Tesla reported Q3 revenue of $25.5 billion, up 5% year-over-year. Vehicle deliveries reached 463,000 units.
